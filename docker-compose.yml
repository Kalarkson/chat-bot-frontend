services:
  frontend:
    container_name: frontend
    restart: unless-stopped
    build:
      context: .
    env_file:
      - .env
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
    volumes:
      - ./logs:/app/logs